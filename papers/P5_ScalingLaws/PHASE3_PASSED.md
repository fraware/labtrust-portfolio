# Phase 3 passed (P5)

Submission-readiness checks were re-run on 2026-04-20 against frozen artifacts generated by:

`PYTHONPATH=impl/src LABTRUST_KERNEL_DIR=kernel python scripts/run_paper_experiments.py --paper P5`

## Verified

- Claim/evidence mapping is synchronized (`claims.yaml` paths exist).
- Repro command is single-path and script-backed.
- Trigger logic uses admissible baselines only (`oracle_baselines` isolated).
- Primary trigger is met for scenario and family holdouts.
- Sensitivity artifact exists (`scaling_sensitivity.json`, caps 10/20/30).
- Recommendation artifact exists (`recommendation_eval.json`).
- Tables and figures are script-generated and regenerated in this run.

## Scope note

`trigger_met` is not uniformly true across all alternative holdout protocols
(regime/agent_count/fault_setting). This is documented in draft limitations and is
not hidden in summary claims.
