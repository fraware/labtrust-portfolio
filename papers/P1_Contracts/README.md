# P1 — Coordination Contracts

**Coordination Contracts: Trace-Derived Admission for Shared CPS State.**

P1 defines a minimal contract layer (typed state, ownership, valid transitions, temporal semantics) that makes coordination pathologies explicit as invalid state transitions and allows a trace-derived validator to detect and deny them before application. Validation is executable from event traces plus declared configuration alone (no privileged hidden state), with bounded per-write overhead.

- **Draft and claims:** [DRAFT.md](DRAFT.md), [claims.yaml](claims.yaml).
- **Outline and scope:** [AUTHORING_PACKET.md](AUTHORING_PACKET.md).
- **Tables and figures:** [VISUALS_PER_PAPER.md](../docs/VISUALS_PER_PAPER.md), [RESULTS_PER_PAPER.md](../docs/RESULTS_PER_PAPER.md).
- **Corpus:** 54+ sequences (tiered benchmark: micro, meso, stress/adversarial). Benchmark spec: [bench/contracts/BENCHMARK_SPEC.v0.1.md](../../bench/contracts/BENCHMARK_SPEC.v0.1.md).
- **Reproducibility:** Minimal and publishable run instructions are in **Appendix A** of the draft; scripts and run manifests are documented there and in the artifact.
- **LADS and transport parity:** Mock LADS demo: `scripts/contracts_mock_lads_run.py`. Transport-invariance parity: `scripts/contracts_transport_parity.py` writes `transport_parity.json` with **`parity_ok_all`** and **`per_sequence[]`** (default stems: `good_sequence`, `split_brain_sequence`, `reorder_sequence`, `actor_payload_writer_mismatch`; override with `--sequences a,b,c`). The contract surface is transport-agnostic by construction; the paper claims **boundary semantic parity** between reference ingestion paths, not full live cross-transport deployment.
- **Run with other papers:** `python scripts/run_paper_experiments.py --paper P1`.
- **Key results:** Corpus detection (54+ sequences including adversarial cases: cross-key interleaving, delayed release/reassignment, concurrent controller races; exact per-event verdict match); policy comparison and **ablation** including `occ_only`, `lease_only`, `lock_only`, `naive_lww` (see draft Section 8–9 and `eval.json` `ablation` / `ablation_by_class`); **`detection_metrics_by_class`** (per-class precision/recall/F1 with uncertainty intervals in `excellence_metrics.per_class_ci95`); event-level latency percentiles with bootstrap CI95; `excellence_metrics` (split_brain_detection_advantage, per_class_ci95); resource_and_cost; scale_test and optional scale_sweep; async stress robustness (`stress_results.json`); transport parity with confidence metrics. Regenerate tables: `python scripts/export_contracts_corpus_table.py` (writes Table 1 + ablation + Table 3 + stress/parity summaries to stdout; commit as `generated_tables.md`). LaTeX appendix longtable: `python scripts/export_p1_appendix_tex.py`. Peer-review figures: `python scripts/render_p1_flow_figure.py`, `python scripts/plot_p1_paper_figures.py`. Bundle: `python scripts/generate_paper_artifacts.py --paper P1`.
