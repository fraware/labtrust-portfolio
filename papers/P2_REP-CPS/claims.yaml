# P2 REP-CPS claim table (Draft conversion checklist 2.1)
# Conditional paper; trigger not met in evaluated scenario. See docs/CONDITIONAL_TRIGGERS.md.
# See docs/DRAFT_CONVERSION_CHECKLIST.md and docs/STATE_OF_THE_ART_CRITERIA.md.

paper_id: "P2_REP-CPS"
title: "REP-CPS: A Safety-Gated Profile for Authenticated Sensitivity Sharing in Cyber-Physical Coordination"
release_id: "rep_cps_eval"

claims:
  - id: "C1"
    statement: "A CPS-oriented sensitivity-sharing profile can make decentralized informational exchange deployable by specifying typed variables, freshness windows, rate limits, provenance, and authentication hooks."
    evidence:
      release_id: "rep_cps_eval"
      confidence: "high"
      artifact_paths:
        - "kernel/rep_cps/PROFILE_SPEC.v0.1.md"
        - "kernel/rep_cps/example_profile.v0.1.json"
        - "datasets/runs/rep_cps_eval/summary.json"
      table_ids: ["Table 1", "Table 6"]
      figure_ids: ["Figure 0"]

  - id: "C2"
    statement: "In the evaluated attack harness, robust aggregation variants reduce compromise-induced bias relative to naive averaging."
    evidence:
      release_id: "rep_cps_eval"
      confidence: "high"
      artifact_paths:
        - "datasets/runs/rep_cps_eval/summary.json"
        - "impl/src/labtrust_portfolio/rep_cps.py"
      table_ids: ["Table 2", "Table 3", "Table 7"]
      figure_ids: []

  - id: "C3"
    statement: "Protocol output is not directly actuating; it influences downstream decisions only through explicit safety-gate mediation compatible with the broader assurance envelope."
    evidence:
      release_id: "rep_cps_eval"
      confidence: "high"
      artifact_paths:
        - "datasets/runs/rep_cps_eval/safety_gate_denial.json"
        - "impl/src/labtrust_portfolio/adapters/rep_cps_adapter.py"
      table_ids: ["Table 1"]
      figure_ids: []

  - id: "C4"
    statement: "The profile can be evaluated reproducibly through MAESTRO-compatible fault injection and compromised-agent suites."
    evidence:
      release_id: "rep_cps_eval"
      confidence: "high"
      artifact_paths:
        - "datasets/runs/rep_cps_eval/summary.json"
        - "scripts/rep_cps_eval.py"
      table_ids: ["Table 1", "Table 2", "Table 3", "Table 5", "Table 6", "Table 7"]
      figure_ids: ["Figure 1", "Figure 2"]

# Tables from summary.json. Figure 1: plot_rep_cps_summary.py (tasks by policy). Figure 2: plot_rep_cps_latency.py (latency by policy). Safety-gate campaign in generated_tables.md.
