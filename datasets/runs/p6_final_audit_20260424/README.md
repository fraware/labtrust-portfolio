Generated by `python scripts/export_p6_final_audit_bundle.py`.

If GPT-5.x directories are missing from the checkout, the audit records that fact explicitly in `gpt5_failure_audit.json` and in this README.

Important interpretation notes:

- Some GPT-5.x run artifacts omit per-run `run_details` for most cases; the audit may expand aggregate `pass_count` deterministically (see `synthetic_pass_bit_ordering`).
- `replay_denials.json` is a frozen summary artifact; a fresh trace scan may count more denied steps if additional `trace.json` files exist under the run directory.
- Optional structured-output reruns are recorded in `gpt5_structured_comparison.json` when present; otherwise the file explains `not_run`.

